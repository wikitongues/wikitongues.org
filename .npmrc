# Suppress npm audit warnings on install.
# All vulnerabilities are in dev dependencies only (eslint, stylus transitive deps).
# The suggested "fixes" are major version downgrades that would break tooling.
# Run `npm audit --omit=dev` to confirm zero production vulnerabilities.
audit=false
