// A bare roll of names. Sized a step below the section heading ($fontScale(6))
// so the list reads as content rather than a run of sub-headings.
.wt_person--name
	width 100%

	h4
		text--header($black)
		font-size $fontScale(5)
		margin 0
